package run

// import "github.com/jaqmol/approx/proc"

// func newConn(from proc.Proc, to proc.Proc) (proc.Conn, error) {
// 	fromOutsLen := len(from.Conf().Outputs())
// 	toInLen := len(to.Conf().Inputs())
// 	if fromOutsLen == 1 && toInLen == 1 {
// 		return NewPipeConn(from, to)
// 	}
// 	return NewNamedPipeConn(from, to)
// }

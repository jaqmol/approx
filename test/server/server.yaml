gateway Server:
  ENV:
    TIMEOUT: 10s
    PORT: 8080

proc getMediaType:
  COMMAND: node get-media-type.js

proc combine:
  COMMAND: node combine.js

flow:
  - Server -> getMediaType -> combine -> Server
  
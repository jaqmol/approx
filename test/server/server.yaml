gateway Server:
  ENV:
    STATIC_PATH: null
    TIMEOUT: 1m
    PORT: 8080

proc readFile:
  ENV.BASE_PATH: $staticBasePath
  COMMAND: node read-file-proc.js

# fork forkReq:
#  ENV.DISTRIBUTE: copy
#
# merge mergeResp: {}

assign:
  $staticBasePath: Server.ENV.STATIC_PATH

flow:
  - Server -> readFile -> Server
  